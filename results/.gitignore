# Ignore shared judged data cache (expensive API calls)
_judged_cache/

# Optionally ignore experiment directories if they become too large
# Uncomment the following lines if experiment results are too large to commit:
*/
!.gitignore
